function inTheBeginning() {

  var heaven = {}, 
      earth;  
  
  let deep = 'darkness';

  if (!earth) {
    
    let light = 'light'; // and there was light
    
    if (light) {
      return deep + ' / ' + light;
    } else {
      earth = deep;
    }
  }

  var evening = day = 43200000; // 12 hours each

  setTimeout(function() {
    return heaven + earth;
  }, evening + day); // one day
}

